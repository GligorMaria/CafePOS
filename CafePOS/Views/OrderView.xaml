<UserControl x:Class="CafePOS.Views.OrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <GroupBox Header="Menu" Margin="5">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding MenuItems}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding Name}" 
                                    Width="120" Height="80" Margin="5"
                                    Command="{Binding DataContext.AddToOrderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>

        <Grid Grid.Column="1" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <GroupBox Header="Current Order">
                <ListBox ItemsSource="{Binding CurrentOrderItems}" DisplayMemberPath="Name" x:Name="OrderList"/>
            </GroupBox>

            <StackPanel Grid.Row="1" Margin="0,10">
                <TextBlock Text="{Binding Total, StringFormat='Total: {0:N2} RON'}" 
                           FontSize="22" FontWeight="Bold" HorizontalAlignment="Right"/>
            </StackPanel>

            <UniformGrid Grid.Row="2" Columns="2" Height="60">
                <Button Content="CASH" Background="#2ECC71" Command="{Binding PayCashCommand}"/>
                <Button Content="CARD" Background="#3498DB" Command="{Binding PayCardCommand}"/>
            </UniformGrid>
        </Grid>
    </Grid>
</UserControl>