<UserControl x:Class="CafePOS.Views.OrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="330"/>
        </Grid.ColumnDefinitions>

        <ScrollViewer VerticalScrollBarVisibility="Hidden">
            <ItemsControl ItemsSource="{Binding MenuItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel HorizontalAlignment="Center"/></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Background="Transparent" BorderThickness="0" Margin="10"
                                Command="{Binding DataContext.AddToOrderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}">
                            <Border Style="{StaticResource MenuCardStyle}" Width="130" Height="130">
                                <StackPanel VerticalAlignment="Center">
                                    <Border Width="50" Height="50" CornerRadius="18" Background="{StaticResource CafeLatte}" Margin="0,0,0,10">
                                        <TextBlock Text="☕" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="{StaticResource CafeDarkBrown}" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding Price, StringFormat='{}{0} RON'}" FontSize="12" Foreground="{StaticResource CafeMediumBrown}" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Border Grid.Column="1" Background="White" CornerRadius="30" Margin="10,5,5,5" Padding="20">
            <Border.Effect><DropShadowEffect BlurRadius="20" Opacity="0.05" ShadowDepth="8"/></Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Current Order" FontSize="20" FontWeight="Bold" Foreground="{StaticResource CafeDarkBrown}" Margin="0,0,0,15"/>

                <ListBox Grid.Row="1" ItemsSource="{Binding CurrentOrderItems}" BorderThickness="0" Background="Transparent">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Name}" Foreground="{StaticResource CafeDarkBrown}" VerticalAlignment="Center"/>
                                <Button Grid.Column="1" Content="✕" Foreground="{StaticResource DangerRed}" Background="Transparent" BorderThickness="0"
                                        Command="{Binding DataContext.RemoveFromOrderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel Grid.Row="2" Margin="0,15">
                    <Separator Background="{StaticResource CafeLatte}" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding Total, StringFormat='{}Total: {0:N2} RON'}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource CafeDarkBrown}" HorizontalAlignment="Right"/>
                </StackPanel>

                <UniformGrid Grid.Row="3" Columns="2" Height="55">
                    <Button Content="CASH" Style="{StaticResource CafeButtonStyle}" Background="{StaticResource CashGreen}" Margin="5" Command="{Binding PayCashCommand}"/>
                    <Button Content="CARD" Style="{StaticResource CafeButtonStyle}" Background="{StaticResource CafeDarkBrown}" Margin="5" Command="{Binding PayCardCommand}"/>
                </UniformGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>