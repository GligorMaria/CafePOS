<Window x:Class="CafePOS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:CafePOS.Views"
        xmlns:vm="clr-namespace:CafePOS.ViewModels"
        Title="Cafe POS"
        Height="650"
        Width="1050"
        Background="{StaticResource CafeBackground}">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <views:DashboardView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:OrderViewModel}">
            <views:OrderView />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- SIDEBAR -->
        <Border Grid.Column="0"
                Background="{StaticResource CafeDarkBrown}"
                CornerRadius="0,35,35,0"
                Padding="20">

            <DockPanel LastChildFill="True">

                <!-- LOGOUT (BOTTOM) -->
                <Button DockPanel.Dock="Bottom"
                        Content="Logout"
                        Click="LogoutButton_Click"
                        Height="44"
                        Margin="0,20,0,0"
                        Background="#806F67"
                        Foreground="White"
                        FontWeight="SemiBold"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="22"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <!-- TOP CONTENT -->
                <StackPanel>

                    <TextBlock Text="CafePOS"
                               Foreground="White"
                               FontSize="28"
                               FontWeight="Bold"
                               Margin="0,10,0,45"
                               HorizontalAlignment="Center"/>

                    <Button Content="New Order"
                            Style="{StaticResource CafeButtonStyle}"
                            Command="{Binding ShowOrderCommand}"
                            Margin="0,10"/>

                    <Button Content="History"
                            Style="{StaticResource CafeButtonStyle}"
                            Command="{Binding ShowHistoryCommand}"
                            Margin="0,10"
                            Opacity="0.75"/>

                </StackPanel>
            </DockPanel>
        </Border>

        <!-- MAIN CONTENT -->
        <ContentControl Grid.Column="1"
                        Content="{Binding CurrentViewModel}"
                        Margin="15"/>
    </Grid>
</Window>
